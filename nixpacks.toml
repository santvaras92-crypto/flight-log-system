# Force clean install and build - Mar 8 2026 v11 - clear webpack cache
[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x", "openssl"]

[phases.install]
cmds = ["rm -rf node_modules/.cache", "npm ci --force"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
